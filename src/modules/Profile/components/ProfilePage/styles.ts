import { theme } from '../../../../theme';
import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  root: {
    padding: '5%',
    flex: 1,
  },
  userInfoList: {
    gap: 14,
  },
  title: {
    marginBottom: 20,
  },
  buttons: {
    flexDirection: 'row',
    gap: 10,
    marginLeft: 'auto',
    marginRight: 'auto',
    marginTop: 20,
  },
});
