import { theme } from '../../../../theme';
import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  root: {
    padding: '5%',
    flex: 1,
  },
  list: {
    marginTop: 20,
  },
  button: {
    marginLeft: 'auto',
    marginRight: 'auto',
  },
});
