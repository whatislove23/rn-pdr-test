import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  root: {
    bottom: -250,
    right: -300,
    position: 'absolute',
    transform: [{ rotate: '-20deg' }],
    width: 1000,
    height: 700,
    zIndex: -1,
  },
});
