import { SafeAreaView } from 'react-native';
import { RatingPage } from '../../modules/Rating';

const Rating = () => {
  return (
    <SafeAreaView style={{ flex: 1, backgroundColor: 'white' }}>
      <RatingPage />
    </SafeAreaView>
  );
};
export default Rating;
